.env
.venv/
__pycache__/
*.pyc
*.pth
*.bin
*.safetensors
/chroma_db/
models/
# hf CLI skills central install cache
.agents/

# Ship the prebuilt ChromaDB knowledge base for deploy (tracked via git-LFS).
# Negations override the *.bin rule above so Chroma's index files are included.
!data/chroma_db/
!data/chroma_db/**

# ICAR PDF corpus — large binaries, ingested locally (keep dir via .gitkeep)
data/pdfs/*.pdf
.pytest_cache/
*.log
dist/
*.egg-info/
.mypy_cache/
.ruff_cache/
.git_file_list.txt
.git_file_list_trimmed.txt

.git_file_list.txt
.git_file_list_trimmed.txt
.git_commit_msg.txt

# Claude Code — personal settings
.claude/settings.local.json
